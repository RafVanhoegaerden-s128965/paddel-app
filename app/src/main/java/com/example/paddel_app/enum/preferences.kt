package com.example.paddel_app.enum

enum class Hand {
    LEFT, RIGHT, AMBIDEXTROUS
}

enum class CourtPosition {
    FOREHAND, BACKHAND
}

enum class MatchType {
    COMPETITIVE, FRIENDLY, BOTH
}

enum class PreferredTime {
    MORNING, AFTERNOON, EVENING, ALL_DAY
}